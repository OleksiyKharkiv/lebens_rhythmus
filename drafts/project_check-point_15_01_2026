MVP теперь full-stack для всех ролей, включая админку. Тогда чек-лист по факту становится следующим, с приоритетом на рабочий функционал для каждой роли:
A. Аутентификация / Пользователи
✅ Логин / регистрация с JWT
✅ Redirect по роли (ADMIN, TEACHER, USER)
✅ Хранение токена и данных пользователя (localStorage)
⚠️ Профиль пользователя: просмотр/редактирование данных (MVP должен включать фронт и бэкенд)
Что нужно проверить / добавить:
Просмотр профиля пользователя
Фронт: страница profile.html или компонент в дэшборде.
Отображать: имя, фамилию, email, роль, активность.
Бэк: эндпоинт GET /users/me (или /users/{id} для админа)
Возвращает текущие данные пользователя.
Проверка JWT + роли.
Редактирование профиля
Фронт:
Форма с полями: firstName, lastName, email (можно редактировать только имя и фамилию; email менять с подтверждением по e-mail).
Кнопка Speichern / Save с проверкой валидности.
Бэк:
Эндпоинт PUT /users/me для редактирования своих данных.
Проверка JWT + валидация данных.
Опционально: админ может редактировать любого пользователя (PUT /users/{id}).
Валидация данных
Проверка на фронте: пустые поля, email формат, минимальная длина имени.
Проверка на бэке: защита от инъекций, дублирующий email.
Обновление токена
При редактировании профиля JWT не меняется, если email не изменился.
Если email редактируется, нужно пересоздать токен или требовать подтверждение.
UI / UX
Кнопка редактирования: показывать только для текущего пользователя и админа.
Уведомления об успешном обновлении (success) или ошибке (error).
Дополнительно для MVP
Админ видит список пользователей (GET /users), с фильтром и кнопкой редактирования.
Учитель/USER видят только свой профиль.
💡 Итог: для MVP нам нужно:
Создать страницу профиля с формой редактирования.
Сделать эндпоинт GET /users/me и PUT /users/me.
Добавить фронт/бек валидацию + уведомления.
Проверить, что JWT защищает доступ к данным.

B. Курсы / Воркшопы
⚠️ CRUD для Workshop и WorkshopGroup
Создание/редактирование/удаление воркшопов (Admin)
Привязка группы к воркшопу и учителю
⚠️ Просмотр списка воркшопов (для всех ролей)
⚠️ Фильтры: по дате, по статусу, по учителю, по возрастной группе
C. Запись на воркшоп (Enrollment)
⚠️ Запись на воркшоп (User)
⚠️ Отмена записи (User)
⚠️ Проверка уникальности (User не может записаться дважды на один воркшоп)
⚠️ Просмотр списка участников (для Teacher/Admin)
D. Админка / Teacher Dashboard
⚠️ Просмотр всех воркшопов и групп
⚠️ Создание и редактирование воркшопов / групп
⚠️ Назначение учителя на группу
⚠️ Просмотр записей участников и статистика по воркшопу
⚠️ Возможность активировать/деактивировать группу
E. Файлы / Документы
⚠️ Загрузка файлов к воркшопу (WorkshopFile)
⚠️ Просмотр / скачивание файлов
⚠️ Ограничение доступа по роли (только участники/админ могут видеть)
F. UX / UI
✅ Мобильное меню
✅ Кнопка показать/скрыть пароль
✅ Уведомления (error/success)
⚠️ Подсветка обязательных полей (*)
⚠️ Адаптивность админки / teacher dashboard
💡 Следующий шаг:
Нужно составить план задач по приоритету, чтобы мы не пытались делать всё сразу. Могу прямо на основе этого чек-листа расписать пошаговую дорожную карту реализации MVP с админкой, включая фронт + бэкенд + эндпоинты.
Хочешь, чтобы я это сделал?
Чтобы собрать MVP полностью, вот чек-лист того, чего пока может не хватать:
A. Фронт
Страница / компонент dashboard для каждой роли (USER, TEACHER, ADMIN)
 Просмотр списка доступных курсов/воркшопов (Workshop)
 Запись на воркшоп (Enrollment)
 Отмена записи на воркшоп
 Профиль пользователя (просмотр / редактирование данных)
 Валидация регистрации (пароль ≥ 6 символов, email формат) — частично есть
 Фронт для администратора/учителя: создание / редактирование воркшопов и групп
B. Бэкенд
 Контроллеры для Workshop, WorkshopGroup, Enrollment, User CRUD
 Эндпоинты для записи / отмены записи на воркшоп
 Проверка уникальности записи (user-workshop) — есть, но фронт должен корректно обрабатывать
 Роли / права доступа (ADMIN / TEACHER / USER)
 Связь Teacher ↔ Group ↔ Workshop корректная (Hibernate баг поправили)
 Файлы / документы (File, WorkshopFile) — пока только таблицы
C. UX / UI мелочи
 Сообщения об ошибках сервера / сети
 Подсветка обязательных полей (*) — уже есть
 Адаптивность страниц (мобильное меню уже есть)
1. Актуальный статус проекта (фактически)
Архитектура
Backend: Spring Boot, JPA, PostgreSQL
Frontend: HTML/CSS/JS (MVP), страницы workshops / activities / dashboard
Инфра: Kubernetes + Helm, Traefik, Cloudflare, домен tlab29.com
Роли: User, Business Owner (Олена), Teacher (пока как отдельная сущность)
Доменные сущности — состояние
Уже есть и в целом ок
User (регистрация, логин, роли)
Workshop — курс как продукт
Group — группа внутри курса
Enrollment — ключевая сущность записи
Activity, AgeGroup, Language, Venue — справочники
WorkshopFile — метаданные файлов
Order — задел под оплату
GlobalExceptionHandler — есть
Проблемные / незавершённые
Enrollment — нет controller/service/repo
Group
нет startDateTime / endDateTime
нет жёсткой связи с Workshop
нет связи Group ↔ Enrollment
Workshop
нет teacher (User)
status не enum
Teacher
дублирует User
нужен рефактор → профиль или отказ
Нет NotificationService
Нет seed-данных для справочников
Нет чёткого admin-flow для Олены
2. Как MVP должен работать (user story — откорректированная)
Роли
User — регистрируется, выбирает курс, записывается
Business Owner (Олена) — создаёт курсы, группы, распределяет людей
Поток MVP
1. Регистрация пользователя
User регистрируется (UserRegistrationDTO)
роль: USER
логин → видит список Workshop
2. Создание курса (Олена)
Олена (роль BUSINESS_OWNER):
создаёт Workshop
name, description
startDate, endDate (LocalDate)
price
status = DRAFT / PUBLISHED
teacher = User (пока может быть сама)
добавляет WorkshopFile (pdf, image)
3. Запись пользователя
User нажимает Enroll
создаётся Enrollment
user
workshop
status = PENDING
group = null
👉 Enrollment — центр MVP
4. Создание групп (Олена)
Олена создаёт Group:
workshop
startDateTime / endDateTime
maxParticipants
activity, ageGroup, language
teacher (User)
статус группы: active
5. Распределение по группам
Олена смотрит Enrollment по Workshop
назначает Enrollment.group
Enrollment.status → CONFIRMED
6. Результат
User видит:
свой Workshop
свою Group
расписание (через Group)
MVP закрыт ✅
3. Что нужно сделать, чтобы завершить MVP
🔴 Критично (без этого MVP не работает)
Enrollment
Repository
Service
Controller
DTO + Mapper
Group
добавить:
startDateTime, endDateTime
@ManyToOne Workshop
@OneToMany Enrollment
Workshop
teacher: User
status: WorkshopStatus enum
User
роль BUSINESS_OWNER проверить / зафиксировать
Seed
Activity
AgeGroup
Language
NotificationService
интерфейс
LogNotificationService (console/log)
🟡 Важно, но можно после
Teacher → OneToOne User или удалить
Payment → пока paymentStatus в Enrollment
Ограничение capacity группы
Валидации (maxParticipants)
4. Ключевая сущность для переноса в новый чат
👉 Enrollment — эталонная
Почему:
связывает User ↔ Workshop ↔ Group
содержит статус, оплату, бизнес-логику
через неё понятна вся модель
Что переносить целиком
Enrollment entity
EnrollmentController
EnrollmentService
EnrollmentRepository
DTO:
EnrollmentCreateDTO
EnrollmentResponseDTO
EnrollmentMapper
Если Enrollment сделан правильно — всё остальное копируется без конфликтов.
5. Короткий «якорь-контекст» для нового чата (можно копировать)
LR App — MVP платформы курсов.
Workshop = курс, Group = расписанная группа, Enrollment = запись пользователя.
User с ролью BUSINESS_OWNER (Олена) создаёт Workshop и Group, распределяет Enrollment по группам.
User регистрируется, выбирает Workshop, создаётся Enrollment (PENDING).
Group содержит start/end DateTime.
Enrollment — центральная сущность.
Следующий логичный шаг:
👉 я пишу Enrollment: controller + service + repo + DTO + mapper (production-ready)
или
👉 сначала добиваем Group entity правильно
скажи, с чего идём дальше.
MVP теперь full-stack для всех ролей, включая админку. Тогда чек-лист по факту становится следующим, с приоритетом на рабочий функционал для каждой роли:
A. Аутентификация / Пользователи
✅ Логин / регистрация с JWT
✅ Redirect по роли (ADMIN, TEACHER, USER)
✅ Хранение токена и данных пользователя (localStorage)
⚠️ Профиль пользователя: просмотр/редактирование данных (MVP должен включать фронт и бэкенд)
Что нужно проверить / добавить:
Просмотр профиля пользователя
Фронт: страница profile.html или компонент в дэшборде.
Отображать: имя, фамилию, email, роль, активность.
Бэк: эндпоинт GET /users/me (или /users/{id} для админа)
Возвращает текущие данные пользователя.
Проверка JWT + роли.
Редактирование профиля
Фронт:
Форма с полями: firstName, lastName, email (можно редактировать только имя и фамилию; email менять с подтверждением по e-mail).
Кнопка Speichern / Save с проверкой валидности.
Бэк:
Эндпоинт PUT /users/me для редактирования своих данных.
Проверка JWT + валидация данных.
Опционально: админ может редактировать любого пользователя (PUT /users/{id}).
Валидация данных
Проверка на фронте: пустые поля, email формат, минимальная длина имени.
Проверка на бэке: защита от инъекций, дублирующий email.
Обновление токена
При редактировании профиля JWT не меняется, если email не изменился.
Если email редактируется, нужно пересоздать токен или требовать подтверждение.
UI / UX
Кнопка редактирования: показывать только для текущего пользователя и админа.
Уведомления об успешном обновлении (success) или ошибке (error).
Дополнительно для MVP
Админ видит список пользователей (GET /users), с фильтром и кнопкой редактирования.
Учитель/USER видят только свой профиль.
💡 Итог: для MVP нам нужно:
Создать страницу профиля с формой редактирования.
Сделать эндпоинт GET /users/me и PUT /users/me.
Добавить фронт/бек валидацию + уведомления.
Проверить, что JWT защищает доступ к данным.

B. Курсы / Воркшопы
⚠️ CRUD для Workshop и WorkshopGroup
Создание/редактирование/удаление воркшопов (Admin)
Привязка группы к воркшопу и учителю
⚠️ Просмотр списка воркшопов (для всех ролей)
⚠️ Фильтры: по дате, по статусу, по учителю, по возрастной группе
C. Запись на воркшоп (Enrollment)
⚠️ Запись на воркшоп (User)
⚠️ Отмена записи (User)
⚠️ Проверка уникальности (User не может записаться дважды на один воркшоп)
⚠️ Просмотр списка участников (для Teacher/Admin)
D. Админка / Teacher Dashboard
⚠️ Просмотр всех воркшопов и групп
⚠️ Создание и редактирование воркшопов / групп
⚠️ Назначение учителя на группу
⚠️ Просмотр записей участников и статистика по воркшопу
⚠️ Возможность активировать/деактивировать группу
E. Файлы / Документы
⚠️ Загрузка файлов к воркшопу (WorkshopFile)
⚠️ Просмотр / скачивание файлов
⚠️ Ограничение доступа по роли (только участники/админ могут видеть)
F. UX / UI
✅ Мобильное меню
✅ Кнопка показать/скрыть пароль
✅ Уведомления (error/success)
⚠️ Подсветка обязательных полей (*)
⚠️ Адаптивность админки / teacher dashboard
💡 Следующий шаг:
Нужно составить план задач по приоритету, чтобы мы не пытались делать всё сразу. Могу прямо на основе этого чек-листа расписать пошаговую дорожную карту реализации MVP с админкой, включая фронт + бэкенд + эндпоинты.
Хочешь, чтобы я это сделал?
Чтобы собрать MVP полностью, вот чек-лист того, чего пока может не хватать:
A. Фронт
Страница / компонент dashboard для каждой роли (USER, TEACHER, ADMIN)
 Просмотр списка доступных курсов/воркшопов (Workshop)
 Запись на воркшоп (Enrollment)
 Отмена записи на воркшоп
 Профиль пользователя (просмотр / редактирование данных)
 Валидация регистрации (пароль ≥ 6 символов, email формат) — частично есть
 Фронт для администратора/учителя: создание / редактирование воркшопов и групп
B. Бэкенд
 Контроллеры для Workshop, WorkshopGroup, Enrollment, User CRUD
 Эндпоинты для записи / отмены записи на воркшоп
 Проверка уникальности записи (user-workshop) — есть, но фронт должен корректно обрабатывать
 Роли / права доступа (ADMIN / TEACHER / USER)
 Связь Teacher ↔ Group ↔ Workshop корректная (Hibernate баг поправили)
 Файлы / документы (File, WorkshopFile) — пока только таблицы
C. UX / UI мелочи
 Сообщения об ошибках сервера / сети
 Подсветка обязательных полей (*) — уже есть
 Адаптивность страниц (мобильное меню уже есть)
1. Актуальный статус проекта (фактически)
Архитектура
Backend: Spring Boot, JPA, PostgreSQL
Frontend: HTML/CSS/JS (MVP), страницы workshops / activities / dashboard
Инфра: Kubernetes + Helm, Traefik, Cloudflare, домен tlab29.com
Роли: User, Business Owner (Олена), Teacher (пока как отдельная сущность)
Доменные сущности — состояние
Уже есть и в целом ок
User (регистрация, логин, роли)
Workshop — курс как продукт
Group — группа внутри курса
Enrollment — ключевая сущность записи
Activity, AgeGroup, Language, Venue — справочники
WorkshopFile — метаданные файлов
Order — задел под оплату
GlobalExceptionHandler — есть
Проблемные / незавершённые
Enrollment — нет controller/service/repo
Group
нет startDateTime / endDateTime
нет жёсткой связи с Workshop
нет связи Group ↔ Enrollment
Workshop
нет teacher (User)
status не enum
Teacher
дублирует User
нужен рефактор → профиль или отказ
Нет NotificationService
Нет seed-данных для справочников
Нет чёткого admin-flow для Олены
2. Как MVP должен работать (user story — откорректированная)
Роли
User — регистрируется, выбирает курс, записывается
Business Owner (Олена) — создаёт курсы, группы, распределяет людей
Поток MVP
1. Регистрация пользователя
User регистрируется (UserRegistrationDTO)
роль: USER
логин → видит список Workshop
2. Создание курса (Олена)
Олена (роль BUSINESS_OWNER):
создаёт Workshop
name, description
startDate, endDate (LocalDate)
price
status = DRAFT / PUBLISHED
teacher = User (пока может быть сама)
добавляет WorkshopFile (pdf, image)
3. Запись пользователя
User нажимает Enroll
создаётся Enrollment
user
workshop
status = PENDING
group = null
👉 Enrollment — центр MVP
4. Создание групп (Олена)
Олена создаёт Group:
workshop
startDateTime / endDateTime
maxParticipants
activity, ageGroup, language
teacher (User)
статус группы: active
5. Распределение по группам
Олена смотрит Enrollment по Workshop
назначает Enrollment.group
Enrollment.status → CONFIRMED
6. Результат
User видит:
свой Workshop
свою Group
расписание (через Group)
MVP закрыт ✅
3. Что нужно сделать, чтобы завершить MVP
🔴 Критично (без этого MVP не работает)
Enrollment
Repository
Service
Controller
DTO + Mapper
Group
добавить:
startDateTime, endDateTime
@ManyToOne Workshop
@OneToMany Enrollment
Workshop
teacher: User
status: WorkshopStatus enum
User
роль BUSINESS_OWNER проверить / зафиксировать
Seed
Activity
AgeGroup
Language
NotificationService
интерфейс
LogNotificationService (console/log)
🟡 Важно, но можно после
Teacher → OneToOne User или удалить
Payment → пока paymentStatus в Enrollment
Ограничение capacity группы
Валидации (maxParticipants)
4. Ключевая сущность для переноса в новый чат
👉 Enrollment — эталонная
Почему:
связывает User ↔ Workshop ↔ Group
содержит статус, оплату, бизнес-логику
через неё понятна вся модель
Что переносить целиком
Enrollment entity
EnrollmentController
EnrollmentService
EnrollmentRepository
DTO:
EnrollmentCreateDTO
EnrollmentResponseDTO
EnrollmentMapper
Если Enrollment сделан правильно — всё остальное копируется без конфликтов.
5. Короткий «якорь-контекст» для нового чата (можно копировать)
LR App — MVP платформы курсов.
Workshop = курс, Group = расписанная группа, Enrollment = запись пользователя.
User с ролью BUSINESS_OWNER (Олена) создаёт Workshop и Group, распределяет Enrollment по группам.
User регистрируется, выбирает Workshop, создаётся Enrollment (PENDING).
Group содержит start/end DateTime.
Enrollment — центральная сущность.
Следующий логичный шаг:
👉 я пишу Enrollment: controller + service + repo + DTO + mapper (production-ready)
или
👉 сначала добиваем Group entity правильно