global:
  imageRegistry: ""   # передаём из CI
  imageTag: ""        # передаём из CI

backend:
  replicaCount: 1
  image:
    repository: backend   # имя image без registry
    tag: ""
  service:
    port: 8080

frontend:
  replicaCount: 1
  image:
    repository: frontend
    tag: ""
  service:
    port: 80