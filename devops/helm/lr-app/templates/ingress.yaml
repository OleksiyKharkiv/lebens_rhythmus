apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lr-ingress
spec:
  rules:
    - host: tlab29.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lr-backend
                port:
                  number: 8080
