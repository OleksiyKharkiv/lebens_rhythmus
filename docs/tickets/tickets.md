# tickets.md — Lebens Rhythmus active backlog

> Единственный активный тикет-трекер (см. `docs/README.md` правило 4).
> Проект был в фризе ~6 месяцев (последний коммит по коду —
> `9a584b0 Backend: Fix : WorkshopService...` и серия "Clean code"), backlog
> заведён заново 2026-07-20 при калибровке новой рабочей сессии. Старый
> `docs/23_10_2025_MVP_tickets` — до-фризовый трекер, не в этой конвенции,
> сверяться при необходимости, не продолжать вести.
>
> Формат: `LR-XXX`, risk tier (LOW/MED/HIGH/INFRA), статус, при закрытии —
> переносить в `archive.md` (per workflow §8 в корневом `CLAUDE.md`).

---

## Спринты (приоритизация)

> Не буквальные time-boxed Scrum-спринты (в проекте нет практики
> спринт-планирования/velocity-трекинга) — волны приоритизации, введены
> 2026-08-08 при ретроспективе бэклога, чтобы "31 открытый тикет плоским
> списком" не значил "всё одинаково срочно". Обновлять при заведении/
> закрытии тикетов, не позволять списку тихо разойтись с реальностью
> тикетов ниже.

**Спринт "Немедленно"** — активный прод-риск прямо сейчас, просроченный
дедлайн, или HIGH-тикет, застрявший на блокере, который стоит
целенаправленно снять:
- **LR-003** — бэкапы Postgres: дедлайн (2026-08-05) уже прошёл, ноль
  бэкапов для данных несовершеннолетних.
- **LR-033** — CORS смешивает прод/dev origins в живом прод-конфиге.
- **LR-034** — verbose DEBUG-логирование активно в проде прямо сейчас.
- **LR-027** / **LR-028** — уже HIGH и уже заведены, но оба зависли на
  "требуется ручной доступ к инфре/GitLab" — приоритет: организовать
  этот доступ, не откладывать бессрочно только потому что технически
  "не заблокировано по вине проекта".

**Спринт "Ближайшее"** — высокая ценность, без внешних блокеров (или
блокер снят прямо сейчас):
- **LR-022 п.6** — финальная верификация аудита. Гейт ("после закрытия
  LR-024..LR-030, LR-021") **уже выполнен** — все эти тикеты закрыты.
  Готово к старту, просто не было замечено как разблокированное до этой
  ретроспективы.
- **LR-035** — rate limiting (прямое расхождение с уже принятым `LR-ADR-007`).
- **LR-036** — workload hardening (`securityContext`/resource limits).
- **LR-039** — обновление `PROJECT_INDEX.md`.
- **LR-041** — шифрование `Participant.birthDate`.
- **LR-037** / **LR-038** — аудит `@Transactional`-границ,
  `DatabaseFixConfig`-проверка.

**Спринт "Плановое / бэклог"** — реальная работа, не срочная:
LR-018, LR-032, LR-040, LR-042 (после продуктового решения), LR-043,
LR-044, LR-005, LR-010, LR-009. LR-016/LR-017 — сознательно отложены
самим заказчиком, не пере-приоритизировать без его запроса.

**Заблокировано** (не может продвинуться без внешнего события —
не игнорировать, просто не в фокусе, пока блокер не снят):
- **LR-045/LR-046/LR-047** — требуют живого доступа к инфре (VM100/
  GitLab admin/Proxmox).
- **LR-001** — дедлайн 2026-08-31, ждёт решения о юридической
  консультации.
- **LR-002 п.5** — сознательно отложенное продуктовое решение
  (single-environment deploy).
- **LR-031 Фаза 3/4** — гейтится на VM600 (`INFRA-008`); **LR-049**
  (круглый стол) проведён и закрыт 2026-08-08 — см. `archive.md` и
  `LR-061`..`LR-063`.

**Обновлено 2026-08-08:** LR-048/LR-049 (оба круглых стола) проведены
и закрыты (`docs/decision-history/roundtable-log.md`, Roundtable
#6/#7) — результат: 11 новых тикетов, `LR-053`..`LR-063`, см. секцию
ниже. Добавить их в волны приоритизации выше по мере уточнения скоупа
каждого — на момент этой записи они ещё не распределены по спринтам,
только заведены.

---

## LR-031 — Структура безопасности + интеграция с мониторингом (VM600) — master-тикет роадмапа

**Tier:** HIGH (auth/authz-observability, сетевая изоляция, кросс-
проектная интеграция)
**Статус:** Open · роадмап согласован (Roundtable #5), реализация не
начата
**Источник:** прямой запрос заказчика 2026-08-06, после LR-022/LR-023 —
"не дешёвый CI-guard, а правильная структура"

Полное обоснование каждого пункта — `docs/decision-history/
roundtable-log.md` (Roundtable #5). Действенный чек-лист —
`docs/security/roadmap.md`. Здесь — только трекинг прогресса по фазам.

**Кросс-проектная зависимость:** Фаза 3 гейтится на реальном
провижининге VM600 (`numi/infra/infra-backlog.md`, INFRA-008 —
"заявлена в топологии, не развёрнута", не в скоупе одного LR).

**Прогресс:**
- [x] Фаза 0 — LR-021, LR-024, LR-025, LR-026, LR-029 (частично — см.
      follow-up LR-032), LR-030 закрыты 2026-08-07, `architect-reviewer`
      approve на весь пакет. LR-027/LR-028 остаются open — блокируются
      на живом инфра-доступе, не могут быть закрыты из этой сессии.
- [x] Фаза 1 — observability-фундамент закрыт 2026-08-07, `architect-reviewer`
      approve (после найденного и исправленного реального пробела в
      CI-guard'е — см. `docs/security/roadmap.md` "Статус" для полного
      разбора отклонений от исходной спецификации). Micrometer/
      Prometheus-эндпоинт, security-метрики в `GlobalExceptionHandler`/
      `AuthService`, двухслойный CI-guard (`ApiSurfaceAllowlistTest`),
      `management.server.port=9090` — все реализованы и покрыты тестами.
- [x] Фаза 2 — сетевая изоляция закрыта 2026-08-08. `NetworkPolicy` в
      `lr-dev` применён живьём с mgmt-core и подтверждён негативным
      тестом (`nc -zv lr-postgres 5432` с пода без нужных меток → exit
      code 1) — закрывает **M4**. По пути был короткий реальный простой
      сайта (неверный порядок применения файлов при первой попытке) —
      откачено, процедура исправлена, повторно применено без инцидента,
      полный разбор в `docs/context/KNOWN_ISSUES.md` и
      `docs/security/roadmap.md`'s "Статус". `networkPolicy.enabled: true`
      закоммичен.
- [ ] Фаза 3 — подключение к VM600 (scrape-джоб, дашборд, alerting rules
      — гейтится на INFRA-008)
- [ ] Фаза 4 — институционализация (периодический повтор аудита, SBOM
      в CI)

**Не начинать Фазу 3 без:** реального провижининга VM600 — координация
с numi-стороной инфра-бэклога, не может быть сделано только из LR.
**LR-049 (круглый стол) проведён и закрыт** (Roundtable #7,
2026-08-08) — результат: `LR-061`/`LR-062`/`LR-063`, учесть при
реализации Фазы 3. Единственный оставшийся гейт — сам VM600.

---

## LR-027 — Scoped DB-роль для приложения (сейчас — суперюзер Postgres)

**Tier:** HIGH (SQL-инъекция/небезопасный native query = ключи от всего
кластера БД, не только таблиц приложения)
**Статус:** Open · требует ручной `psql`-сессии против прода (не может
быть сделано мной)
**Источник:** LR-022, фаза 2 (см. `docs/security/audit-2026-08-06.md`,
H4). Расширяет уже открытый `LR-003` пункт 6 (backup-роль) на runtime-
креденшл самого приложения — более ценную из двух целей.

`backend-deployment.yaml` и `pg-statefulset.yaml` используют один и тот
же Secret `lr-db-credentials` — то, что `initdb` делает bootstrap-
суперюзером. Приложение аутентифицируется в Postgres суперюзером для
всего обычного трафика.

**Сделать:** создать scoped-роль `lr_app` (без `SUPERUSER`/`CREATEDB`/
`CREATEROLE`) и отдельную `lr_backup` (read-only), перевести креды,
разнести секреты.

---

## LR-028 — Scoped CI kubeconfig (сейчас — вероятно full cluster-admin)

**Tier:** HIGH · **Статус:** Open · сначала требует живой проверки
(GitLab project Settings → CI/CD → Variables), не может быть подтверждено
из файлов репозитория
**Источник:** LR-022, фаза 2 (см. `docs/security/audit-2026-08-06.md`, H5)

`.gitlab-ci.yml`'s `deploy-dev` использует `$KUBECONFIG_FILE` — по
единственной задокументированной конвенции получения (`INFRA-LR.md`
§10) это копия `/etc/rancher/k3s/k3s.yaml`, встроенный cluster-admin
конфиг k3s. Ни одного namespace-scoped `ServiceAccount`/`RoleBinding`
для CI нет нигде в репозитории.

**Не начинать фикс без:** живой проверки, что `$KUBECONFIG_FILE`
реально настолько широкий, как предполагает эта единственная
задокументированная процедура — может быть уже уже, чем кажется из
файлов.

---

## LR-022 — DevSecOps-аудит: backend/infra/frontend, "круглый стол" экспертов + протокол в docs/security/

**Tier:** HIGH (вся граница auth/authz, инфра-доступ к серверу и БД,
потенциальные SQLi/обход бэкенда — максимальный блэст-радиус на проекте)
**Статус:** Open · активная разработка (внеочередной, вне обычной
приоритизации — по прямому запросу заказчика 2026-08-06, сразу после
LR-014/LR-020, которые и стали поводом)
**Источник:** прямой запрос заказчика 2026-08-06, после того как в этой
же сессии дважды нашлись реальные, ранее незамеченные проблемы
безопасности (LR-014 timing side-channel, LR-020 — Bean Validation не
работал нигде в бэкенде вообще)

**Формат — "круглый стол"** (установленная в проекте конвенция:
именованные реальные фигуры индустрии, транслирующие свою публичную
методологию, не выдуманные цитаты) специально под этот аудит, состав и
методологии — по заданию заказчика:

- **Katie Moussouris** (Luta Security) — координированное раскрытие
  уязвимостей, устранение корневых архитектурных причин, а не подсчёт
  багов.
- **Tim Mackey** (Synopsys) — software supply chain risk, полная
  трассируемость build-пайплайна.
- **Chris Wysopal** (Veracode) — риски AI-generated кода, runtime-
  валидация поведения, не только статический анализ.
- **Rob Winch** (Spring Security Lead) — архитектура `SecurityFilterChain`,
  многоуровневая авторизация (`@PreAuthorize` + URL-правила),
  deny-by-default, OAuth2/JWT.
- **Josh Long** (Spring Developer Advocate) — security as a first-class
  property: stateless JWT, ролевая/scope-based авторизация, secure
  defaults (BCrypt/TLS/CSRF), zero-trust между сервисами.

**Методологии:** Security by Design, Zero Trust Architecture, NIST SSDF
(SP 800-218), OWASP Secure by Design, Threat Modeling (STRIDE/DREAD/
Attack Trees).

### Скоуп (6 пунктов, задание заказчика дословно)

1. Ревизия всего бэкенда на паттерны/уязвимости, включая всё, что
   касается авторизации (`@PreAuthorize`, `SecurityConfig`, JWT, границы
   `ADMIN`/`BUSINESS_OWNER`/`TEACHER`/`USER`).
2. Ревизия инфра-файлов и деплоя — дыры, через которые можно
   проникнуть на сервер/к БД (Helm/K8s манифесты, secrets-хендлинг,
   `.gitlab-ci.yml`, сетевые правила, доступ к Postgres).
3. Ревизия всего фронтенда — возможные SQL-инъекции, и отдельно —
   **есть ли прямые обращения из фронта напрямую к БД в обход бэкенда**
   (архитектурно не должно быть по ADR, но нужно реально проверить, не
   поверить на слово).
4. По результатам — протокол в новой `docs/security/` (имя файла с датой
   аудита), с общим результирующим уровнем архитектурной безопасности +
   перечнем необходимых изменений.
5. По протоколу — завести тикеты на устранение найденных дыр/уязвимостей.
6. После закрытия этих тикетов — повторить пп.1-4 как финальную
   верификацию (не в этом заходе — гейтится на закрытии тикетов из п.5).

**Базовые артефакты для анализа** — используются одновременно и как
источник правды об архитектуре, и как потенциальный источник
уязвимостей (проверять на архитектурные решения, которые могли
неосознанно создать дыры или неэффективность):
`docs/architecture/erm.drawio`, `docs/architecture/decisions.md`,
`docs/architecture/IMPLEMENTATION-PROTOCOL-2026-07.md`,
`docs/architecture/lr-erm-2026-07.drawio`, `docs/context/PROJECT_INDEX.md`.

**Прогресс:**
- [x] П.1 — backend (готово 2026-08-06, три фоновых агента параллельно)
- [x] П.2 — infra/deploy (готово 2026-08-06)
- [x] П.3 — frontend (готово 2026-08-06)
- [x] П.4 — протокол в `docs/security/audit-2026-08-06.md` (готово
      2026-08-06; 1 CRITICAL, 5 HIGH, 6 MEDIUM, 9 LOW/INFO)
- [x] П.5 — тикеты по результатам: LR-024..LR-030 заведены (см. выше),
      LR-021 уже был заведён отдельно (Payment/Order DTO validation).
      **LR-023 — CRITICAL-находка п.1, устранена немедленно вне очереди
      п.4/5, см. `archive.md`** — Spring Data REST давал полный обход
      авторизации + само-эскалацию до ADMIN
- [ ] П.6 — финальная верификация (после закрытия LR-024..LR-030,
      LR-021 — не начата)

---

## LR-018 — Bootstrap первого админа: только через ручной psql, нет env-var/seed-механизма

**Tier:** LOW (dev-ops/onboarding gap, не риск для существующих данных)
**Статус:** Open · backlog, не блокирует ничего
**Источник:** вопрос заказчика "как сделать юзера админом" 2026-08-06

`PUT /users/{id}/role` (`UserController.java:158`) и admin-UI
(`admin/users/+page.svelte`) корректно реализуют смену роли — но оба
требуют `hasRole('ADMIN')`, то есть работают только когда админ уже
существует. Самый первый админ на чистом окружении (новый staging,
DR-восстановление, локальная разработка) сегодня назначается только
прямым `UPDATE users SET role = 'ADMIN' WHERE email = ...` через
`kubectl exec` в Postgres-под — ручная, недокументированная в коде
операция (описана только в истории чата/CHANGELOG, не в runbook).

**Направление** (не решено окончательно, требует выбора заказчика):
- Env-var-gated bootstrap при старте приложения: если `role=ADMIN`
  юзеров в БД ноль и задан `BOOTSTRAP_ADMIN_EMAIL` — повысить этого юзера
  (если уже зарегистрирован) при старте `main()`/через `ApplicationRunner`.
  Не создаёт юзера с нуля (пароль не через код) — только повышает роль
  уже существующему аккаунту, что проще и безопаснее.
- Альтернатива попроще: просто задокументировать текущую psql-команду в
  `docs/runbooks/` (bootstrap-checklist для нового окружения) — не менять
  код вообще, если механизм psql устраивает как разовая операция.

**Не начинать реализацию без:** решения заказчика, какой из двух путей
нужен (код vs документация) — второй вариант дешевле и, возможно,
достаточен на этом масштабе проекта (один owner, редкие новые окружения).

---

## LR-016 — Автоматическая проверка конфликтов бронирования зала (low priority)

**Tier:** MED (бизнес-логика, без персональных данных)
**Статус:** Open · **сознательно не MVP** — отложено до момента, когда
Olena освоится создавать воркшопы вручную (сама формулировка заказчика)
**Источник:** Круглый стол #3, 2026-08-05 (пункт "2.5")

Как только `venue`/`room` появятся на уровне `Group` (LR-015), технически
возможно проверять: не забронирован ли уже конкретный `(venue_id, room)`
на пересекающийся временной интервал другой активной группой. Сейчас —
осознанно не строим (не overengineering ради фичи, которой некому
пользоваться, пока ручной поток создания воркшопов не обкатан). Не
начинать без явного запроса — заказчик сам скажет, когда актуально.

---

## LR-017 — Механизм подтверждения участия/оплаты (М2/М3 зависят от этого)

**Tier:** MED (продуктовое решение с архитектурными последствиями,
не персональные данные повышенной чувствительности)
**Статус:** Open · блокирует точность М2 (выручка) и М3 (заполняемость)
— обе метрики пока используют "сырую регистрацию" как прокси
**Источник:** Круглый стол #3, 2026-08-05

Открытый вопрос заказчика: "как получить подтверждение участия юзера, не
загоняя его в кабалу 'оплати или умри', но с ~80% уверенностью, что
зарегистрировавшиеся реально придут/оплатят?" Также увязано с М2 —
реальная выручка требует интеграции с Numi + автоматизации
счетов/реквизитов (карта/Stripe предпочтительнее IBAN для
украиноязычной аудитории — сами по себе отдельная интеграционная
задача, DOCS/INTEGRATION-тир по конвенции проекта).

**Направление, предложенное Круглым столом #3** (не финальное решение,
входная точка для дальнейшей проработки): email-подтверждение намерения
за N дней до старта (тот же паттерн токена/ссылки, что уже реализован
для email-верификации регистрации, LR-013) — дешёвый, реализуемый сейчас
сигнал, качественно отличный от "просто зарегистрировался", но не
требующий платёжной интеграции. Настоящая оплата (Stripe/Numi) — большая
отдельная задача, не в этом тикете.

**Не начинать реализацию без:** явного решения заказчика по конкретному
флоу (какой дедлайн до старта, что происходит с неподтверждённой
регистрацией — авто-снятие брони или просто пометка для Olena).

---

## LR-001 — DSGVO/GoBD-ревизия юридических страниц

**Tier:** DOCS/COMPLIANCE (аналог numi HIGH — данные детей, юридический риск)
**Статус:** Open · **Срок:** конец августа 2026 (2026-08-31)
**Источник:** калибровочная сессия 2026-07-20

Юридические страницы физически существуют (`frontend/pages/impressum/{impressum,agb,datenschutzerklaerung,widerruf}.html`),
но это заглушки — не проходили полноценную юридическую ревизию под текущее
законодательство (DSGVO, GoBD) и текущую бизнес-модель LR (данные детей —
возрастные группы, вероятно ФИО и контакты опекунов, возможно медицинские
заметки для физактивности — Art. 8/9 DSGVO).

**Объём работы (по решению заказчика — один из путей или комбинация):**
- Консультация с юристом по DSGVO/GoBD для творческих студий с
  несовершеннолетними участниками, ИЛИ
- Заказ готовых юридических артефактов через специализированный сервис
  (например e-recht24.de) с адаптацией под LR,
- Полный аудит текущих текстов на импрессуме/датеншутц/widerruf/AGB,
- "Юридический пентест" — намеренная попытка найти в текущих формулировках
  дыры, которыми могла бы воспользоваться реальная претензия/жалоба, до
  того как это сделает кто-то другой.

**Не делать без этого тикета:** не пытаться самостоятельно "дописать"
юридический текст без консультации — см. `CODING_PROTOCOL.md` Datenschutz-check,
он явно помечен как черновик-заглушка, не юридическая консультация.

---

## LR-002 — Переработка CI/CD (`.gitlab-ci.yml`) под новый фронтенд + DB-миграции

**Tier:** INFRA
**Статус:** Open · блокирующий для фронтенд-перестройки (см. круглый стол)
**Источник:** калибровочная сессия 2026-07-20

Текущий `.gitlab-ci.yml` работает, но собран под старый статический
фронтенд.

**Закрыто в сессии 2026-07-21/22 (после круглого стола):**
1. ~~Тесты не запускаются в CI~~ — не устранено в CI (пайплайн по-прежнему
   вызывает только `./gradlew bootJar`), но проверено локально: 7/7
   backend-тестов зелёные (Testcontainers + Flyway против реального
   Postgres), см. `CHANGELOG.md` 2026-07-22. Само подключение к CI —
   остаётся ниже, в новом пункте 4.
2. ~~Мёртвый Helm-параметр `flannel.backend=host-gw`~~ — убран из
   `.gitlab-ci.yml`.
3. ~~Нет ни одного migration-шага~~ — Flyway подключён, `DatabaseFixConfig`
   удалён, `V1__baseline.sql` есть и проверен против реального Postgres
   (LR-ADR-003, `docs/architecture/decisions.md`).

**Закрыто в сессии 2026-07-23:**
3b. ~~`npm run build` падал~~ — `adapter-static` требовал `prerender=true`
   на всех 23 маршрутах (главная, все каталоги, dashboard/admin/teacher).
   Причина: весь фронтенд грузит данные клиентски через `$effect`, никогда
   через SvelteKit `load()`, и часть страниц принципиально динамическая
   (auth-gated). Решение — SPA fallback mode
   (`adapter({ fallback: 'index.html' })` в `vite.config.ts`), а не
   форсировать prerender там, где он не подходит по архитектуре страниц.
   `npm run build` теперь зелёный, `npm run preview` подтверждён вживую
   (прямой заход на `/workshops`, не через клиентскую навигацию —
   fallback-механизм реально отрабатывает).

**Закрыто в сессии 2026-07-23 (продолжение):**
4. ~~CI не собирает и не тестирует `frontend-svelte`~~ — новый job
   `test-frontend` (stage `build`, `node:22-alpine`: `npm ci && npm run
   check && npm run test`) реально гейтит `docker-frontend` через `needs:`
   — не повторяет ошибку из пункта 1 (тесты существуют, но никогда не
   выполняются). `docker-frontend` переключён с `cd frontend` →
   `cd frontend-svelte` — старый статический сайт больше не собирается и
   не деплоится вообще, он полностью заменён. Playwright e2e по-прежнему
   сознательно не подключён (LR-ADR-020) — добавить в этот же job, когда
   появится, не отдельным тикетом.
4b. ~~Nginx должен отдавать SPA fallback~~ — новый `frontend-svelte/
   nginx.conf` (`try_files $uri $uri/ /index.html;`) + `frontend-svelte/
   Dockerfile` (multi-stage: `node:22-alpine` → `nginx:alpine`).
   Проверено вживую не через `vite preview` (у него fallback уже
   встроенный, не показательно), а через **реальный собранный Docker-образ**:
   `docker build` + `docker run`, `curl` напрямую (без единой строчки JS)
   на `/`, `/workshops`, `/admin/users`, `/dashboard` — везде 200, отдаётся
   SPA-shell с правильными `_app/immutable/...`-ассетами. Образ и контейнер
   удалены после проверки (`lr-frontend-test`), в реестр не пушились.

**Остаётся открытым:**
5. Single-environment: весь пайплайн деплоит в единственный `lr-dev`
   namespace, нет staging/prod разделения — осознанно (единственный
   пользователь, Olena) на сегодня, но стоит явно решить, остаётся ли так
   при масштабировании после запуска.

**Зависит от:** ничего — все пункты кроме 5 (single-environment,
осознанное решение на будущее, не блокер) закрыты.

---

## LR-003 — Бэкапы и репликация PostgreSQL (`lr-postgres`)

**Tier:** INFRA
**Статус:** Interim-решение готово в коде (Helm), **не применено на
кластере** — блокер на пользователе (см. "Осталось сделать" ниже).
**Срок применения interim-решения: 2026-08-05** (создать IDrive e2
bucket/credentials, задеплоить, провести реальный тест бэкапа —
пункты 1-4 чек-листа ниже; п.5 restore — тоже до этой даты, если
получится). Финальное решение (backup-контроллер) — отдельно, near-term,
привязано к апгрейду домашнего сервера (план — сентябрь 2026).
**Источник:** калибровочная сессия 2026-07-20; interim-реализация 2026-07-24;
срок 2026-08-05 назначен заказчиком 2026-07-24

Решение по обоим блокирующим вопросам получено от заказчика 2026-07-24:
хранилище — **IDrive e2** (тот же провайдер, что у numi Litestream,
отдельный bucket/credentials), механизм — **Kubernetes CronJob** (не
systemd на VM200/lr700, как изначально предполагал тикет — идиоматичнее
для k3s-хостящегося Postgres).

**Реализовано** (`devops/helm/lr-app/`):
- `templates/postgres-backup-cronjob.yaml` — CronJob `lr-postgres-backup`,
  `postgres:16-alpine` (уже содержит `pg_dump`), `restic` ставится на
  лету через `apk` (не заводить отдельный кастомный образ ради
  временного решения). `concurrencyPolicy: Forbid` +
  `activeDeadlineSeconds: 1800` — зависший прогон не блокирует
  расписание навсегда.
- `templates/postgres-backup-configmap.yaml` — сам скрипт (`pg_dump` →
  `restic init` (no-op если уже есть) → `restic backup` → `restic forget
  --prune`, retention 14 daily / 8 weekly).
- `values.yaml`: секция `postgresBackup` (schedule, repository, retention)
  + документация нужного `kubectl create secret lr-backup-secrets`
  (restic-password/access-key-id/secret-access-key — по той же конвенции,
  что `lr-backend-secrets`).
- **Проверено вживую** (не на проде — Docker-контейнер `postgres:16-alpine`
  локально): `pg_dump`/`restic` оба доступны и работают в этом образе;
  реальный прогон script'а против тестовой БД — `pg_dump` → `restic init`
  → `restic backup` → `restic forget --prune` — все шаги отработали
  успешно, снапшот реально создан. `helm template` — 0 ошибок, ConfigMap
  и CronJob рендерятся корректно, `enabled: false` корректно убирает оба
  ресурса.
- Процедура восстановления добавлена в
  `docs/runbooks/infra-fix-shutdown.md` — **ни разу не проверена на
  реальном кластере**, тот же урок, что уже был у numi/Litestream
  (бэкап без проверенного восстановления не доказан рабочим) — первый
  реальный прогон восстановления должен случиться при первой
  возможности, не откладывать.

**Осталось сделать (не может быть сделано мной — реальная прод-инфра):**
1. Создать bucket в IDrive e2 для LR (отдельный от numi).
2. `kubectl -n lr-dev create secret generic lr-backup-secrets ...` (см.
   `values.yaml` комментарий) — реальные credentials, не для передачи в
   чат.
3. Указать `postgresBackup.repository` в `values.yaml` (реальный
   bucket-URL) и задеплоить (`helm upgrade`).
4. Хотя бы один раз проверить `kubectl get cronjob`/`kubectl logs`, что
   первый реальный прогон в проде действительно создал снапшот в IDrive e2.
5. Прогнать restore-процедуру хотя бы раз (не обязательно поверх прод-БД
   — можно в тестовом namespace) — см. новый раздел в
   `infra-fix-shutdown.md`.

**Follow-up, найдено architect-reviewer 2026-07-24 (не блокирует, но не
забывать):**
6. Backup-job переиспользует `lr-db-credentials` (полные права владельца
   БД) — для `pg_dump` достаточно read-only роли. Отдельной роли в
   проекте нет нигде, создание требует ручной `psql`-сессии против
   прод-БД. Не блокирует запуск, но стоит сделать до серьёзного аудита
   TOM/DSGVO.
7. Нет алертинга при падении ночного backup-job'а — узнать можно только
   `kubectl get cronjob`/`kubectl logs` вручную. Отдельная, б о́льшая
   задача (интеграция с Grafana/мониторингом), не в скоупе interim-решения.

---

## LR-005 — `PaymentMapper.toSelfViewDTO()`: перейти с block-листа на allow-лист

**Tier:** LOW (tech debt, не баг сегодня)
**Статус:** Open · backlog, не блокирует ничего
**Источник:** architect-reviewer, ревью LR-004, 2026-07-23

`toSelfViewDTO()` сейчас строит полный admin DTO через `toResponseDTO()` и
обнуляет `note` — безопасно для единственного известного сегодня поля, но
инвертирует безопасный default: любое новое admin-only поле, добавленное в
будущем в `Payment`/`PaymentResponseDTO`, молча утечёт через `/payments/me`,
если про него забудут вспомнить именно здесь. Правильное решение — строить
self-view DTO явным allow-листом полей (или тест, что self-view DTO
field-set — строгое подмножество admin DTO field-set), вместо блокирования
известных полей по одному. Не переделывать сейчас — текущий скоуп (скрыть
`note`) решён корректно и просто — почистить при добавлении следующего
чувствительного поля в `Payment`.

---

## LR-032 — Заменить 59 мест `RuntimeException("X not found")` на типизированное not-found-исключение с единообразным 404

**Tier:** LOW (статус-код неверный, но контент больше не течёт — LR-029
уже закрыл реальную утечку)
**Статус:** Open · backlog, не блокирует ничего
**Источник:** follow-up к LR-029, `architect-reviewer` подтвердил
разумность отдельного скоупа, 2026-08-07

59 мест в 18 сервисных файлах используют голый `orElseThrow(() -> new
RuntimeException("X not found with id: " + id))` → падает в общий
catch-all → 500 вместо семантически верного 404. Реальная утечка
контента уже закрыта (LR-029) — это чисто про корректность статус-кода,
не про security. Требует отдельного, внимательного прохода по всем 18
сервисам (свой `architect-reviewer`, свои тесты), не точечный патч —
заведено отдельно, а не тихо расширен скоуп LR-029.

---


## LR-010 — Удалить мёртвый `frontend/` + обновить доки, описывающие фронтенд как статический

**Tier:** LOW (docs/cleanup, без риска)
**Статус:** Open · backlog, не блокирует ничего
**Источник:** architect-reviewer, ревью LR-002 пп.4/4b, 2026-07-23

С 2026-07-23 CI больше не собирает и не деплоит `frontend/` (переключено
на `frontend-svelte`, см. `CHANGELOG.md`) — старая директория и её
`Dockerfile` никем не референсятся (проверено ревьюером по всему
`devops/`), но физически всё ещё в репозитории. Также
`docs/context/PROJECT_INDEX.md` и `docs/infra/INFRA-LR.md` всё ещё
описывают фронтенд как "статический HTML/CSS/JS" — устарело.

**Сделать:** удалить `frontend/` (после подтверждения, что круглый стол
про снос старого сайта уже окончательный — сверить с
`docs/decision-history/roundtable-log.md`), обновить оба дока под
реальный стек (SvelteKit). Не срочно — не блокирует ничего, чисто
гигиена репозитория.

---

## LR-009 — `GroupController`/`Group`: смена workshop у существующей группы при редактировании

**Tier:** MED (не HIGH — не auth, не деньги, но затрагивает существующие
enrollments) · **Статус:** Open · backlog
**Источник:** построение admin-панели Groups, 2026-07-23, найдено при
чтении `GroupService.update()`

`GroupService.update()` до этой сессии не копировал `startDateTime`/
`endDateTime` вообще (исправлено, см. `CHANGELOG.md` 2026-07-23) — но
`workshop` реассайнмент при редактировании существующей группы
по-прежнему не поддержан ни в сервисе, ни в новой admin-странице
(намеренно). Вопрос не решён: если у группы уже есть enrollments,
привязанные к конкретному workshop/времени, что должно происходить с
ними при смене workshop у группы задним числом? Не решать по умолчанию —
нужен явный ответ заказчика, прежде чем добавлять эту возможность в
`GroupService.update()` и в UI.

---

# Ретроспектива бэклога 2026-08-08 — новые тикеты

> Полная сверка `tickets.md`+`archive.md` против `docs/security/
> audit-2026-08-06.md`, `docs/architecture/decisions.md`,
> `docs/infra/INFRA-LR.md`, `docs/context/PROJECT_INDEX.md`,
> `docs/decision-history/roundtable-log.md`. Найдены находки аудита без
> тикетов (M5/M6/M9/M10), решения ADR без исполнения (LR-ADR-012),
> задокументированные-но-не-заведённые follow-up'ы (birthDate), и
> инфра-долг без трекинга (`INFRA-LR.md` §7/§8,
> `PROJECT_INDEX.md` §6). LR-033..LR-047 ниже — результат. Полный разбор
> расхождений (что уже закрыто, что устарело в самих источниках) — см.
> `CHANGELOG.md`, запись от 2026-08-08.

---

## LR-035 — Нет application-level rate limiting нигде — прямое расхождение с `LR-ADR-007`

**Tier:** HIGH (не по факту эксплуатации сегодня, а по стандарту, который
проект сам себе установил и не выполняет)
**Статус:** Open
**Источник:** LR-022, находка M10; `docs/architecture/decisions.md`
LR-ADR-007 явно ставит "приоритет автоматизированным атакам и crypto
hygiene выше insider-угроз" — на практике это означает rate-limiting
должен был появиться раньше многих уже закрытых тикетов, а не отсутствовать
вовсе

Ни на `/auth/register`, ни на `/auth/login`, ни на
`/auth/resend-verification` нет ограничения частоты запросов —
brute-force/credential-stuffing/email-bombing доступны без всякого
барьера. Направление (не решено окончательно): Bucket4j
(in-memory, single-instance — подходит, пока `lr-backend` в одном
реплике) на уровне фильтра/interceptor, ключ — по IP + по email из тела
запроса (не только по IP — иначе один атакующий с ротацией IP всё ещё
проходит). Метрика `auth_login_failure_total{reason}` (LR-031 Фаза 1)
уже даёт наблюдаемость всплеска — этот тикет добавляет реальный барьер,
не только видимость.

---

## LR-036 — Workload hardening: `securityContext` отсутствует везде (root), нет `resources.requests/limits`

**Tier:** MED (защита в глубину — не даёт прямого RCE сама по себе, но
снижает blast radius любого будущего RCE/container-escape, и снимает
DoS-соседство между подами одной ноды)
**Статус:** Open
**Источник:** LR-022, находка M5 + LOW-находка "no resources.requests/
limits" (`docs/security/audit-2026-08-06.md`)

`backend-deployment.yaml`, `frontend-deployment.yaml`,
`pg-statefulset.yaml`, `backend/Dockerfile` — ни один workload не
объявляет `securityContext` (контейнеры стартуют root) и ни один не
объявляет `resources.requests/limits` (шумный сосед на той же ноде
может забрать всю память/CPU). Объединено в один тикет — оба правятся
в тех же файлах, естественно один диф. Начать с `runAsNonRoot: true`
+ `allowPrivilegeEscalation: false` на всех трёх Deployment/StatefulSet,
подобрать `requests`/`limits` по реальному потреблению (`kubectl top
pods -n lr-dev` перед тем, как гадать числа).

---

## LR-037 — Аудит границ `@Transactional` — ни разу не проводился

**Tier:** MED (не найдена конкретная живая ошибка — но это и есть риск:
класс багов, который не виден без целевого прохода, частичные записи
при сбое посреди многошаговой операции)
**Статус:** Open
**Источник:** Roundtable #1, 2026-07-20/21 (`docs/decision-history/
roundtable-log.md`) — явно вынесено как открытый пункт "полный аудит
`@Transactional`-границ — не сделан вообще", никогда не было заведено
отдельным тикетом с момента круглого стола

Пройти по всем `@Service`-методам, которые пишут больше одной сущности
(например `EnrollmentService`/`GroupService`/`PaymentService`), и
проверить: (а) есть ли `@Transactional` там, где реально нужен атомарный
многошаговый write, (б) нет ли `@Transactional` там, где он создаёт
слишком широкую транзакцию вокруг внешнего вызова (SMTP, будущий
Stripe/Numi) — что держит соединение с БД открытым на время
сетевого I/O.

---

## LR-038 — Проверить, не дублирует/не конфликтует ли `DatabaseFixConfig` с Flyway-миграциями

**Tier:** MED (потенциальный конфликт двух независимых механизмов
изменения схемы — раньше единственный механизм, теперь, возможно,
избыточный/опасный рядом с настоящими миграциями)
**Статус:** Open
**Источник:** `docs/context/PROJECT_INDEX.md` DEBT-1 +
`docs/context/KNOWN_ISSUES.md` (задокументированный готча "реальная
схема может не совпадать с файлами миграций")

`DatabaseFixConfig` меняет схему БД в рантайме при каждом старте
приложения (`ALTER TABLE workshop_groups DROP NOT NULL` на нескольких
колонках) — механизм существовал до того, как в проект был введён
Flyway (`LR-ADR-003`, `V1__baseline.sql`). Не проверено: содержит ли
теперь baseline-миграция то же самое изменение схемы (тогда
`DatabaseFixConfig` — мёртвый, но потенциально конфликтующий код,
трогающий реальные колонки при каждом старте без всякой пользы), или
изменение по-прежнему нужно только через рантайм-патч. Разобраться и
либо удалить `DatabaseFixConfig` (если миграция уже покрывает это),
либо перенести его логику в настоящую Flyway-миграцию (если ещё нет).

---

## LR-039 — `docs/context/PROJECT_INDEX.md` заметно устарел относительно реального состояния репозитория

**Tier:** MED (документ существует специально для восстановления
контекста "с нуля" в новой сессии — его собственная неактуальность
подрывает именно эту функцию, тот же класс риска, что и обоснование для
создания `docs/security/ARCHITECTURE.md` в numi/LR ранее в этой сессии)
**Статус:** Open
**Источник:** ретроспектива бэклога 2026-08-08 — сверка PROJECT_INDEX.md
§2/§3/§6/§7/§9/§11 против фактического состояния кода/доков

Файл сам себя маркирует как черновик ("составлено без прямого доступа к
репозиторию... TODO подтвердить путь" по всей структуре), но с момента
его написания случилось: полная миграция фронтенда на SvelteKit (файл
всё ещё называет его "статический HTML/CSS/JS"), введение Flyway
(§11 всё ещё спрашивает "TODO: локальный инстанс или туннель к прод?"
про БД, не упоминая миграции вообще), полный набор LR-ADR-001..020,
весь цикл LR-022 security-аудита + `docs/security/ARCHITECTURE.md`. §5
"Архитектурные инварианты" по-прежнему помечен как ПРЕДПОЛАГАЕМЫЕ, не
верифицированы — спустя множество реальных `architect-reviewer`-ревью,
которые могли бы их подтвердить. Пройти по всему файлу секция за
секцией, свериться с реальным кодом/доками, убрать устаревшие TODO там,
где ответ уже давно есть в другом документе (ссылкой, не дублированием).

---

## LR-040 — Исполнить уже принятое `LR-ADR-012`: перенести `cf_worker_lr.js` в decision-history

**Tier:** LOW-MED (решение уже принято, не требует новой дискуссии —
но файл содержит реальный публичный IP+порт, годами живущий в "боевой"
директории `devops/`, хотя воркер подтверждённо неактивен)
**Статус:** Open
**Источник:** `docs/architecture/decisions.md` LR-ADR-012 ("Decision:
файл перенести в docs/decision-history/ как исторический, не активную
инфру — сам перенос НЕ сделан в рамках этого ADR"); подтверждено
`docs/security/audit-2026-08-06.md`'s LOW-находкой №4

Решение принято, исполнение — нет. Переместить `cf_worker_lr.js` из
"боевой" `devops/cloud_flare/` (или где он физически лежит сейчас) в
`docs/decision-history/`, с коротким комментарием "неактивен, оставлен
для истории топологии". Перед переносом — реальный, а не
предполагаемый, port-scan/проверка, что воркер и правда нигде не
задеплоен (audit-doc отметил это как непроверенное).

---

## LR-041 — `Participant.birthDate` хранится plaintext — задокументированный, но никогда не заведённый follow-up к LR-011

**Tier:** MED (дата рождения ребёнка — идентифицирующие данные
несовершеннолетнего, повышенная чувствительность по тому же принципу,
что уже привёл к шифрованию других PII-полей в LR-011/e66adbf)
**Статус:** Open
**Источник:** `docs/security/audit-2026-08-06.md`, LOW-находка №1,
явно описанная как "уже задокументированный follow-up к LR-011" — но
такого тикета физически не существовало нигде в бэклоге/архиве

Применить `EncryptedStringConverter` (тот же паттерн, что уже на
`User`/`Teacher`/`Participant`'s остальных PII-полях) к
`Participant.birthDate`. Учесть: если поле участвует в
сортировке/фильтрации по возрасту где-либо в коде — та же оговорка, что
уже задокументирована для `User.email` (GCM даёт разный шифротекст на
одинаковый вход, ломает `WHERE`/`ORDER BY` по значению) — проверить
использование поля в запросах перед применением конвертера, не
предполагать по аналогии с другими полями.

---

## LR-042 — `Contract.partyName`/`Contract.contact` не зашифрованы — нужно осознанное решение о скоупе

**Tier:** LOW-MED (нужен продуктовый/юридический выбор прежде чем
кодить — B2B-контакт может быть ниже чувствительности, чем PII детей,
но текущее состояние непоследовательно с политикой проекта для
"contact"-типа полей)
**Статус:** Open · блокируется на решении, какие именно поля `Contract`
достаточно чувствительны, чтобы шифровать
**Источник:** `docs/security/audit-2026-08-06.md`, LOW-находка №2

Не начинать реализацию без явного ответа: `Contract` — это про
взрослых-контрагентов (преподаватели-подрядчики, поставщики) или может
содержать данные, эквивалентные по чувствительности PII участников?
Если да — применить `EncryptedStringConverter` тем же паттерном, что и
везде; если нет — явно задокументировать здесь и в
`docs/security/ARCHITECTURE.md` §2.7, почему `Contract` сознательно вне
периметра шифрования, чтобы это не всплывало как "необъяснённая
непоследовательность" в каждом следующем аудите.

---

## LR-043 — Security/supply-chain гигиена: BCrypt cost, консолидация JWT-ключа, image-pinning

**Tier:** LOW (три независимых, но однотипных по размеру находки —
объединены в один тикет, чтобы не раздувать бэклог тремя тикетами по
одной строке кода каждый)
**Статус:** Open · backlog, не блокирует ничего
**Источник:** `docs/security/audit-2026-08-06.md`, LOW-находки №3, №5, №7

1. `BCryptPasswordEncoder` использует implicit default cost (10) —
   сделать явным `new BCryptPasswordEncoder(12)`, учитывая профиль
   чувствительности проекта (дети + IBAN/налоговые номера).
2. `JwtUtils` и `SecurityConfig`'s `jwtDecoder` независимо деривируют
   HMAC-ключ из `JWT_SECRET` — не эксплуатируемо сегодня, но хрупко при
   будущем рефакторинге одного без другого; свести к одному источнику.
3. `curlimages/curl:latest` (мутируемый тег) в CI, образы без pin на
   digest — `docker-frontend`/`docker-backend` уже используют
   commit SHA (хорошо), но `:latest` в CI-утилитах приглашает будущий
   обход трассируемости.

---

## LR-044 — `revisionHistoryLimit` не задан в Helm templates — старые ReplicaSets копятся бесконечно

**Tier:** LOW (гигиена кластера, не риск данных/доступа — но НЕ
блокируется на живом инфра-доступе, чисто Helm-темплейт, можно
реализовать и провалидировать `helm template`/`helm lint` в любой
сессии)
**Статус:** Open · backlog
**Источник:** `docs/infra/INFRA-LR.md` LR-BUG-04

Добавить `revisionHistoryLimit: 3` (или похожее небольшое число) в
`spec` `backend-deployment.yaml` и `frontend-deployment.yaml` —
дефолтное поведение k8s хранит ВСЕ старые ReplicaSet-объекты
бессрочно.

---

## LR-045 — Инфра-документация/чистка: адрес origin в `cloudflared`, неиспользуемый `caddy.service`

**Tier:** LOW · **Статус:** Open · требует живого доступа к VM100 (не
может быть сделано из этой сессии, тот же класс блокировки, что у
LR-027/LR-028)
**Источник:** `docs/infra/INFRA-LR.md` LR-BUG-06 + `docs/context/
PROJECT_INDEX.md` DEBT-2

1. Прочитать `/etc/cloudflared/config.yml` на VM100, записать реальный
   origin-адрес явно в `INFRA-LR.md` (сейчас документ ссылается на файл,
   не на его содержимое).
2. `caddy.service` на VM100 существует, но не используется (реальный
   прокси — `cloudflared`) — удалить сервис или задокументировать
   явную причину, почему он остаётся (например "оставлен как fallback
   на случай Х" — если такой причины нет, просто убрать, чтобы не
   путать при инцидент-débugging).

---

## LR-046 — Автоматизировать обновление GitLab registry token (сейчас истекает молча)

**Tier:** MED (уже вызывало реальный простой — симптом "`ImagePullBackOff`
разом по всем подам `lr-dev`" уже случался и задокументирован) · **Статус:**
Open · требует живого доступа к GitLab admin/VM
**Источник:** `docs/context/KNOWN_ISSUES.md` + `docs/context/
PROJECT_INDEX.md` DEBT-5

Токен для `gitlab-registry` Secret в `lr-dev` истекает без
автоматического продления — узнаётся только постфактум по массовому
`ImagePullBackOff`. Направление (не решено): CronJob, который
регенерирует токен и патчит Secret до истечения, либо переход на
GitLab Deploy Token с более длинным/настраиваемым сроком жизни вместо
персонального access token (если сейчас используется именно он —
проверить вживую, не предполагать).

---

## LR-047 — Установить `qemu-guest-agent` на VM100/VM200/VM300 — упрощает recovery-сценарии

**Tier:** MED (не риск сегодня, но снижает возможности recovery именно
в момент реального инцидента — тот же класс важности, что и остальные
recovery-процедуры в `infra-fix-shutdown.md`, учитывая, что у проекта
уже была реальная история из 7 независимых поломок после одного power
outage) · **Статус:** Open · требует живого доступа к Proxmox-хосту
**Источник:** `docs/context/PROJECT_INDEX.md` DEBT-4

Без `qemu-guest-agent` гипервизор не может корректно определить
готовность/состояние гостевой VM (graceful shutdown, IP-репортинг) —
усложняет и удлиняет recovery по `docs/ops/infra-fix-shutdown.md` в
реальном инциденте. Установить на все три VM основного k3s-кластера.

---

# Тикеты по итогам Roundtable #6/#7 (2026-08-08)

> Оба круглых стола проведены (`docs/decision-history/roundtable-log.md`),
> прошли ревью `architect-reviewer` на архитектурную релевантность —
> фактических ошибок не нашлось, один реальный архитектурный промах
> исправлен до финализации (см. запись Roundtable #6, пункт Harris/п.3
> синтеза — `load()` изначально недооценивал реальную цену вопроса) и
> несколько пробелов дополнено (VM300 dual-role, Telegram-бот адъясентность,
> уже решённый на #5 статус VM600-архитектуры). Тикеты ниже — прямое
> следствие финальных синтезов, не механически "один пункт эксперта =
> один тикет".

---

## LR-053 — Единая навигация: убрать дублирование desktop/mobile-разметки

**Tier:** LOW-MED (UX-баг класса "уже стрелял" — iPhone 14
stuck-menu-инцидент — не гипотетический риск)
**Статус:** Open
**Источник:** Roundtable #6 (`docs/decision-history/roundtable-log.md`),
синтез п.1 — Harris + Nielsen + Wroblewski независимо пришли к одному
выводу

`src/routes/+layout.svelte` содержит два раздельных блока разметки
навигации (desktop/mobile) с идентичным набором пунктов вместо одного
источника данных, отрендеренного дважды. Свести к одному списку пунктов
меню (`{#each}` в обоих местах, или единый блок с CSS-only responsive
через `<details>`) — убирает не только дублирование кода, но и целый
класс багов вроде уже случившегося "зависшего" мобильного меню.

---

## LR-054 — Секционные loading/error состояния на admin- и личном дашборде

**Tier:** LOW-MED
**Статус:** Open
**Источник:** Roundtable #6, синтез п.2 — Nielsen, "Visibility of
system status"

`admin/+page.svelte` и `dashboard/+page.svelte` оба используют один
общий loading/error gate поверх нескольких параллельных fetch'ей — если
падает один запрос, не видно вообще ничего вместо частичной деградации.
Перевести на секционные состояния — каждая секция (stats/recent
workshops/alerts/fill-rate/trend/retention на admin; schedule/media/
payments на личном дашборде) показывает свою ошибку/загрузку
независимо от остальных.

---

## LR-055 — Пересмотреть `LR-002` (SPA-fallback) целиком: стоит ли SSR/`load()`

**Tier:** MED (архитектурный вопрос с реальной ценой — смена адаптера,
переработка Docker/nginx-образа и k3s-деплоя, не мелкая доработка)
**Статус:** Open · **не начинать без явного решения, что скоуп именно
такой ширины, не "узкое исследование `load()`"**
**Источник:** Roundtable #6, синтез п.3 — Harris; скорректировано
`architect-reviewer`'s находкой: `hooks.server.ts`/`load()` не
исполняются на реальном проде под текущим `adapter-static`
(`vite.config.ts`, `nginx.conf`'s `try_files ... /index.html`)

Осознанный отказ от `load()` в пользу client-side fetch (`LR-002`,
SPA-fallback вместо prerender) был обоснован тем, что многие страницы
auth-gated и динамические. Вопрос, который стоит явно поставить: стоит
ли цена смены адаптера (`adapter-node`/`adapter-cloudflare` и т.п.) +
переработки прод-образа ради SSR/streaming для дашбордов конкретно, или
секционные loading-состояния (`LR-054`) уже закрывают практическую
часть проблемы в рамках текущей SPA-модели без этой цены. Не начинать
реализацию без явного ответа на этот вопрос — не путать с `LR-054`,
которая достижима без пересмотра `LR-002`.

---

## LR-056 — Admin-дашборд: группировка sub-nav по workflow + иерархия "требует внимания"

**Tier:** LOW-MED
**Статус:** Open
**Источник:** Roundtable #6, синтез п.4 — Cooper, goal-directed design

Два связанных изменения на одной странице, один диф:
1. `admin/+layout.svelte`'s sub-nav — плоский список из 8 пунктов
   (Overview/Users/Activities/Workshops/Groups/Venues/AgeGroups/
   Performances) без группировки. Сгруппировать по реальному
   admin-workflow (например "Каталог": Activities/Workshops/
   Performances; "Операции": Groups/Venues/AgeGroups; "Люди": Users) —
   не по алфавиту/порядку добавления в код.
2. `admin/+page.svelte` смешивает "требует действия сейчас" (alerts,
   fill-rate warnings) и "как идут дела у бизнеса" (registration trend,
   retention) на одном плоском экране без иерархии — первое должно быть
   визуально доминирующим и первым, второе — второстепенным блоком.

---

## LR-057 — Мобильный вид Payments-таблицы: карточки на узких экранах

**Tier:** LOW
**Статус:** Open
**Источник:** Roundtable #6, синтез п.5 — Wroblewski, mobile-first

`dashboard/+page.svelte`'s Payments-секция — сырая HTML `<table>`, не
компонент. На узком экране это либо горизонтальный скролл, либо
нечитаемо сжатые колонки. Карточный вид (как остальные секции того же
дашборда) на мобильных, таблица — progressive enhancement на широком
экране.

---

## LR-058 — Точечный a11y-аудит: bar chart + гамбургер-кнопка

**Tier:** LOW-MED (не общий аудит всего сайта — два конкретных элемента)
**Статус:** Open
**Источник:** Roundtable #6, синтез п.6 — Soueidan. Severity-бейджи
(изначально тоже под вопросом) **проверены `architect-reviewer` и уже
корректны** (текстовая подпись уровня рядом с цветом,
`admin/+page.svelte`'s `levelLabel`) — не входят в скоуп этого тикета.

1. Registration trend (`admin/+page.svelte`, самодельный CSS bar
   chart) — проверить и, при отсутствии, добавить текстовый эквивалент
   для скринридера (голые `div` с inline `height%` без `aria-label`,
   скорее всего, ничего не сообщают).
2. Гамбургер-кнопка мобильного меню (`+layout.svelte`) — целевой аудит
   на `aria-label`/`aria-expanded`, приоритет выше обычного: этот
   конкретный элемент уже один раз давал реальный прод-баг (iPhone 14,
   зависшее меню).

---

## LR-059 — Вынести `Table`/`Badge` в переиспользуемые компоненты

**Tier:** LOW (не срочно — явный порог "повторилось 3+ раз")
**Статус:** Open · backlog
**Источник:** Roundtable #6, синтез п.7 — Harris + Nielsen (consistency)

Таблицы (`dashboard/+page.svelte` payments) и бейджи (severity-метки,
роль-индикаторы) собраны inline на каждой странице заново — нет
`Table`/`Badge` в `src/lib/components/`. Не тащить тяжёлый UI-кит
(осознанно, Harris) — но вынести именно эти 2 паттерна в компоненты
сейчас, до того как повторение случится в очередной раз и разъедется
мелкими расхождениями (радиус, отступы).

---

## LR-060 — Проверить номенклатуру Activities/Workshops/Performances с заказчиком

**Tier:** LOW (продуктовый вопрос, не инженерный — блокируется на ответе)
**Статус:** Open · требует ответа заказчика/Olena, не решается в одиночку
**Источник:** Roundtable #6, синтез п.8 — Nielsen, "Match between
system and real world"

Три раздельных пункта меню (`Activities`/`Workshops`/`Performances`)
совпадают со схемой БД (`Activity → Workshop → Group`), но не
проверено, совпадает ли это с тем, как реальный взрослый посетитель
мысленно делит предложение студии, или это просто проекция схемы БД
наружу. Не менять без прямого разговора с заказчиком/Olena — вопрос
не решается дизайном в вакууме.

---

## LR-061 — Задокументировать общий контракт именования security-метрик (кросс-проектно)

**Tier:** MED (кросс-проектная координация — LR ведёт свою половину,
numi должна подтвердить свою)
**Статус:** Open
**Источник:** Roundtable #7 (`docs/decision-history/roundtable-log.md`),
синтез п.1 — Volz

`LR-031`'s метрики (`auth_login_failure_total{reason}` и т.д.) не
должны переименовываться в `lr_auth_login_failure_total` при
подключении к VM600 — разграничение между проектами уже даётся
бесплатно через `job`-лейбл `scrape_configs`. Написать явный документ
("shared metrics naming contract"): какие метрики держат общее имя/
семантику там, где концепция общая (login-failures, unmapped-paths,
authz-denials) — и явное правило про cardinality (не тегировать
raw-путём/user_id "just for debugging", тот же принцип, что `LR-031`
уже применил для `authz_denied_total`/`http_unmapped_path_total`).
LR документирует свою (Spring/Micrometer) половину сама (Long, тот же
роундтейбл) — координация с numi-стороной нужна для общей части, не
может быть закрыта только из LR.

---

## LR-062 — Выбрать и подключить канал уведомлений для алертинга (Telegram-бот, по умолчанию)

**Tier:** MED (блокирует реальную пользу от всего `LR-031` Фазы 3 —
алерт без канала доставки бесполезен)
**Статус:** Open · требует подтверждения заказчика (или явного отказа
от дефолта ниже)
**Источник:** Roundtable #7, синтез п.2 — Fong-Jones — "самая
конкретная и самая срочная точка всего роундтейбла"

Канал уведомлений не решён нигде — ни для numi, ни для LR, при том что
это не забытый пункт, а явно задокументированный открытый вопрос в
обоих проектах уже давно. Дефолт-кандидат: Telegram-бот через
встроенный Grafana Cloud contact point (просто, бесплатно, push на
телефон, подходит для single-operator, не 24/7 on-call команды).
**Отдельный бот/токен** от уже упомянутого в `LR-ADR-016` кандидата на
Telegram как канал клиент-facing напоминаний — не переиспользовать
один и тот же токен для обеих задач (разная аудитория, разная модель
риска). Не строить полный каталог алертов раньше, чем доставка
проверена живьём хотя бы один раз — v1: 2-3 alert'а на проект
("сработал — смотреть сегодня"), список уже есть в `docs/security/
roadmap.md` Фазы 3.

---

## LR-063 — Требование к VM600: self-monitoring + осознанное размещение (не на перегруженной роли)

**Tier:** MED (гейтит корректность всей идеи "единая нода" — если
VM600 сама тихо ляжет, все три проекта теряют мониторинг одновременно,
возможно незамеченно)
**Статус:** Open · применяется при провижининге VM600 (`INFRA-008`,
не в скоупе одного LR) — заведено здесь как требование к будущей
инфраструктуре, не как самостоятельная задача, выполнимая уже сейчас
**Источник:** Roundtable #7, синтез пп.3-4 — Gregg, USE-метод
(Utilization/Saturation/Errors)

Требования к будущему провижинингу VM600:
1. Host-level self-monitoring самой ноды — CPU/память/диск, здоровье
   процесса коллектора, дропнутые/неудачные скрейпы — не только быть
   каналом для остальных трёх проектов.
2. `node_exporter` на всех VM (100/200/300/600/700), не только
   app-level метрики — иначе видно "приложение X тормозит", но не
   "потому что у ноды кончилась память" (уже реально случавшийся класс
   проблемы в этом гомлабе — NAT-правило, перегрузка gateway-core).
3. **Не размещать VM600 на уже перегруженной роли.** Явно два
   кандидата, которых избегать: `gateway-core` (NAT + cloudflared +
   k3s worker, `LR-BUG-05`) и **VM300** (прод-хост numi + k3s-worker
   для workout-evo — сегодня безопасно только потому, что workout-evo
   намеренно остановлен; при реальном запуске workout-evo станет тем
   же классом риска — решить до, не после).

---

## LR-064 — `spring.jpa.show-sql=true` логирует SQL безусловно, не через уровень логирования

**Tier:** LOW-MED (тот же класс риска, что M9, другой механизм —
`show-sql` не проверяет `logging.level.*` вообще, использует
Hibernate/`System.out`-путь напрямую)
**Статус:** Open · backlog
**Источник:** `architect-reviewer`, ревью LR-034, 2026-08-08

`spring.jpa.show-sql=true` (`application.properties:6`) печатает сырой
SQL безусловно, независимо от того, что `LR-034` только что понизил
`logging.level.*` — SQL-запросы (потенциально с параметрами через
Hibernate-форматтер) продолжат литься в прод-логи как есть. Решить:
выключить полностью (`false`), или перевести на `logging.level.org.
hibernate.SQL=${LOG_LEVEL_SQL:WARN}` (правильный Hibernate-идиоматичный
способ логировать SQL через настоящий логгер, с контролем уровня, а не
через `show-sql`'s собственный неконтролируемый путь).

---

# Эпик "Курсы" — LR-065..LR-079 (2026-08-09)

> Прямой срочный запрос заказчика (Olena) — юридически связан с
> `docs/compliance/tlab29-zfu-compliance-brief.md` (ZFU/FernUSG-заявка
> для онлайн-курса "Theaterlabor"), деловая срочность — родители готовы
> записываться после каникул, нужна публичная страница описаний курсов.
> **Не "пожар"** — совпадение трёх независимых поводов, не аварийная
> ситуация. Полный контекст решений — `LR-ADR-021`/`022`
> (`docs/architecture/decisions.md`), ERM — `docs/architecture/erm.drawio`.
>
> **Критический путь до живой публичной страницы курса:** LR-066(done)
> → LR-067 → **LR-068 (круглый стол)** → LR-069 → LR-075 → LR-076 → LR-077.
> Всё остальное — либо параллельно, либо после.

---

## LR-065 — Курсы: новая сущность + мульти-день расписание + ZFU-комплаенс — master-тикет эпика

**Tier:** HIGH (новая доменная сущность, пересекает ARCH-границы
domain/service/repository/controller, юридически значимый контент)
**Статус:** Open · роадмап согласован, реализация начата (ERM/ADR готовы)
**Источник:** прямой запрос заказчика, 2026-08-09

Полное обоснование архитектурных решений — `LR-ADR-021`
(`Course` — отдельная сущность) и `LR-ADR-022` (`Session` под `Group`
для мульти-дня), оба в `docs/architecture/decisions.md`. Здесь — только
трекинг прогресса по подтикетам.

**Прогресс:**
- [x] LR-066 — ERM/ADR-коррекция (`Course` расформирован из
      "Workshop/Course", `Session` добавлена, `courseId`-связи
      зафиксированы) — закрыт 2026-08-09.
- [x] LR-067 — Backend: сущность `Session` (дочерняя `Group`) — закрыт
      2026-08-09, `architect-reviewer` approve with changes (нашёл и
      закрыт до мержа пробел в тестовом покрытии — реальный
      integration-тест на orphanRemoval).
- [x] LR-068 — **Круглый стол**: модель расписания/периодичности
      `Course` — закрыт 2026-08-09, `architect-reviewer` approve with
      changes (5 находок, все закрыты правкой протокола/`LR-ADR-023`
      до принятия, не отложены). Итог: `Course` без полей расписания;
      `Group` несёт `courseId`+правило повторения; `Session` (`LR-067`)
      материализует занятия. Породил `LR-081`..`LR-084` (см. ниже).
- [x] LR-069 — Backend: сущность `Course` — закрыт 2026-08-09, вместе с
      LR-075/LR-076/LR-078 в рамках одного прямого запроса заказчика
      ("начинай создавать Course сразу с полями/сервисами/фронтэндом").
- [x] LR-070 — Backend: `Workshop.courseId` — закрыт 2026-08-11.
- [x] LR-071 — Backend: `Performance.courseId` — закрыт 2026-08-11.
- [x] LR-072 — Backend: `Workshop.teacher` → `Teacher` (было `User`) —
      закрыт 2026-08-11.
- [x] LR-073 — Frontend: admin-страница Teachers — закрыт 2026-08-11.
- [ ] LR-074 — Frontend: форма создания Workshop — мульти-день + fix teacher-дропдауна
- [x] LR-075 — Frontend: форма создания Course — закрыт 2026-08-09.
- [x] LR-076 — Frontend: публичный каталог/страница Course — закрыт
      2026-08-09.
- [ ] LR-077 — Контент: Theaterlabor — ZFU-комплаенс текст (требует
      согласования точных формулировок с заказчиком перед публикацией)
- [x] LR-078 — Frontend: правки AGB (§9/§10/§11 добавлены дословно по
      брифу) — закрыт 2026-08-09. §3.4 (VAT-флаг для бухгалтера) остаётся
      вне кода.
- [ ] LR-079 — Backlog (не срочно): форма согласия на съёмку
      (Einwilligungserklärung) для Aufführungen — "резерв на будущее"
      по формулировке брифа, не начинать без отдельного запроса
- [x] LR-081 — Backend: расширение `Group` (`courseId` + правило
      повторения) — закрыт 2026-08-12 (коммит `fa6bfc5`).
- [x] LR-082 — Backend: `SessionService` — генерация `Session` из
      правила повторения `Group` — закрыт 2026-08-12 (коммит `fa6bfc5`).
- [x] LR-083 — Backend: mutual-exclusivity guard `workshopId`/`courseId`
      в `GroupService` — закрыт 2026-08-12 (коммит `fa6bfc5`).
- [x] LR-084 — Backend+Frontend: `Enrollment`/`EnrollmentService`/
      `EnrollmentController`/`Order` — единая регистрация на
      `Workshop`+`Course` — закрыт 2026-08-16 (коммит `f706ae8`), см.
      archive.md для полного описания (реализация вышла существенно
      шире исходного текста — atomic-capacity fix, Order security fix,
      open-redirect fix, cleanup-джоб).
- [x] LR-085 — Frontend: recurrence-UI для Course-связанных групп —
      закрыт 2026-08-12, но **не так, как описано в тикете** (см. archive.md
      — реализовано на `admin/courses/+page.svelte` по прямой инструкции
      заказчика, не на `admin/groups/+page.svelte`).

---



## LR-074 — Frontend: форма создания Workshop — мульти-день расписание

**Tier:** MED-HIGH (пользовательский UX-flow, задействует новую
`Session`-сущность)
**Статус:** Open · **блокируется на `LR-067` (Session backend, закрыт)**
· teacher-дропдаун уже починен в `LR-072` (переключён на `GET
/teachers`), не в скоупе этого тикета
**Источник:** прямой запрос заказчика п.4

`admin/workshops/+page.svelte`: убрать единственную пару `Start`/`Ende`,
заменить на "Количество дней" (число, макс. 10) в конце формы → при
изменении генерируется N под-форм, каждая с `Start`/`Ende`
(datetime-local, точность до минуты) + `Место проведения` (select по
`Venue`). Teacher-дропдаун переключить с `getAllUsers().filter(role)`
на `getTeachers()` (реальный `Teacher`-эндпоинт).

---

## LR-077 — Контент: страница курса "Theaterlabor" — ZFU-комплаенс текст

**Tier:** HIGH (юридически значимый контент, напрямую влияет на активную
переписку с госорганом — ZFU) · **Статус:** Open · разблокирован
(`LR-075`/`LR-076` закрыты) · **финальный текст требует согласования с
заказчиком перед публикацией — не публиковать по собственному решению**
**Источник:** `docs/compliance/tlab29-zfu-compliance-brief.md`

Контент страницы курса "Theaterlabor" (создаётся через `LR-075`'s форму
как данные, не хардкод) должен обязательно содержать (бриф §2):
только для взрослых, живая видеоконференция синхронно, занятия НЕ
записываются, нет "посмотреть позже", нет экзаменов/оценок/сертификата/
диплома, цель — досуг/творчество/личностное развитие. Категорически
не должно быть: "Aufzeichnung"/"on-demand"/"jederzeit abrufbar" и
аналогов, "Zertifikat"/"Abschluss"/"Qualifikation"/"Prüfung"/"Diplom",
упоминаний асинхронных материалов/домашних заданий с проверкой.

**Рекомендация из брифа, взята за основу:** держать эти формулировки в
одном источнике (поле в `Course`, не размазанный по нескольким местам
текст) — единый источник для сайта, чтобы не разъехаться с текстом
письма в ZFU.

**Не публиковать без:** явного подтверждения точного текста заказчиком
— бриф прямо запрещает менять формулировки про запись/не запись без
согласования, т.к. это напрямую влияет на активную переписку с ZFU.

---

## LR-079 — Backlog: форма согласия на съёмку (Einwilligungserklärung) для Aufführungen

**Tier:** LOW (явно помечено в брифе как "резерв на будущее") · **Статус:**
Open · backlog, не начинать без отдельного запроса заказчика
**Источник:** `docs/compliance/tlab29-zfu-compliance-brief.md` §3.3

Отдельный документ/чекбокс согласия на фото/видео/аудио-съёмку при
регистрации на Aufführungen (не Theaterlabor) — нужен только когда
реально начнётся практика записи выступлений, не сейчас.

---

## LR-080 — `File` и `WorkshopFile` — похоже, дублирующие сущности

**Tier:** LOW-MED (не риск сегодня — оба реально работают, но
неочевидно, какую использовать для новой функциональности, легко
случайно завести данные не в той таблице)
**Статус:** Open · backlog
**Источник:** сверка устаревшей `docs/architecture/lr-erm-2026-07.drawio`
с реальным кодом при работе над эпиком "Курсы", 2026-08-09

Обе сущности реально существуют и полностью реализованы (свой
`Repository`/`Service`/`Controller`/`Mapper` у каждой): `File`
(таблица `files`) и `WorkshopFile` (таблица `workshop_files`) — обе
`@ManyToOne` на `Workshop`. `Workshop.files` (сама коллекция на
сущности) смотрит на `WorkshopFile`, `File` держит свою FK на Workshop,
но не замаплена обратно как коллекция на `Workshop` — то есть `File`
физически "сирота" со стороны `Workshop`, обращение к ней идёт только
через `FileController`/`FileRepository` напрямую.

**Разобраться:** какая из двух — актуальная, какая — забытый более
ранний вариант (обе полностью рабочие, не сразу очевидно по коду, кто
"главная"). Консолидировать в одну, либо явно задокументировать, зачем
нужны обе (например, `File` — для чего-то не привязанного к Workshop
изначально, просто ещё не расширенного). Не начинать удаление без
проверки реальных данных в обеих таблицах на проде.

---

## LR-086 — Admin `Groups`: нет поля языка — редактирование молча обнуляет `Group.language`

**Tier:** LOW (чистый frontend, один `<select>`, без риска для данных
кроме самого поля, которое и так уже молча теряется)
**Статус:** Open · backlog
**Источник:** `architect-reviewer`, ревью диффа артефакт-аудита
2026-08-14 (закрытие mass-assignment дыры на `GroupController.updateGroup`)

`Group.language` (`@ManyToOne`, читается в `GroupDTO.languageId`) не
имеет соответствующего `<select>` ни на `admin/groups`, ни на
`admin/courses` — оба места никогда не отправляли `languageId` при
создании/редактировании. Это не регрессия сегодняшнего фикса
(`GroupUpdateDTO`/`GroupUpdateRequestDTO`): при старом raw-entity-биндинге
поведение было идентичным — `language` в присланном JSON всегда
отсутствовал, значит всегда обнулялся при `update()`. Просто раньше это
было незаметно за общей дырой, а сейчас `GroupService.update()`
корректно (authoritative-паттерн) явно обнуляет `language`, когда
`languageId` не пришёл — то есть теперь это единственная причина
обнуления, а не побочный эффект мусорного биндинга.

**Сделать:** добавить `<select>` языка на `admin/groups/+page.svelte`
(и, если применимо, на `admin/courses`), прокинуть `languageId` через
`toCreateRequest`/`toUpdateRequest`. Либо — если поле реально не нужно
продукту прямо сейчас — явно решить и задокументировать, что `Group.
language` не используется, а не оставлять тихо повреждающимся полем.

---

## LR-087 — Продажа билетов на Performance через `Order` (заблокировано на регистрации Gewerbe)

**Tier:** LOW (пока не актуально для бизнеса)
**Статус:** Open · **заблокировано** — Олена сейчас работает по §19 UStG
(Kleinunternehmer), для продажи билетов юридически нужно открыть Gewerbe
(предприятие) в Германии — этого пока нет
**Источник:** LR-084, круглый стол по регистрации 2026-08-16 —
владелец явно вынес из скоупа LR-084

Когда Gewerbe появится, билет на Performance концептуально ближе к
`Order` (коммерческая транзакция), не к `Enrollment` (участие) —
покупателем может быть частное лицо (себе или в подарок другу) либо
предприятие/фирма. Реализовать тогда же, заодно решить found this
session:

- `Event.java` (полный CRUD-стек: Controller/Service/Repository/Mapper)
  нигде не используется во фронтенде (проверено `grep` — ноль
  вхождений) — мёртвый дубль `Performance`. `Order.event_id` указывает
  на этот мёртвый `Event`, не на реальный `Performance` — сегодня в
  `Order` в принципе нет пути на настоящий перформанс. Решить это —
  часть той же задачи, не отдельно.
- `Performance` не имеет `price`-поля вообще (в отличие от `Event`,
  у которого есть) — понадобится при реализации.

**Не начинать без:** явного решения владельца, что Gewerbe открыта и
продажа билетов актуальна.

---

## LR-088 — Участник-ребёнок (`Participant`) при регистрации на Course/Workshop — согласие родителей

**Tier:** LOW (нет текущего прецедента)
**Статус:** Open · backlog
**Источник:** LR-084, круглый стол по регистрации 2026-08-16 —
владелец явно вынес из скоупа LR-084

Сегодня `Enrollment` — только `User` (юзер регистрируется сам на
себя), поля `Participant` нет вообще (в отличие от `Order`, где
`participant`-поле уже есть — асимметрия, найдено круглым столом).
Прецедента пока нет, но в планах — курсы/воркшопы для подростков; в
Германии это лица до 21 года, требуется согласие родителей
(compliance-требование, не техническое). Явно зафиксировано владельцем
как "не в LR-084" — реализовать отдельным тикетом, когда появится
реальный сценарий.

**Не начинать без:** реального курса/воркшопа для несовершеннолетних в
планах + согласованного с владельцем механизма родительского согласия
(не изобретать самостоятельно — юридический вопрос).

---

## LR-089 — Ревизия всех request-DTO на предмет клиент-управляемых статусов/флагов без пере-авторизации

**Tier:** MED (сама ревизия — чтение/анализ; отдельные находки могут
эскалироваться до HIGH в зависимости от того, чего касаются)
**Статус:** Open · backlog
**Источник:** LR-084, круглый стол по регистрации 2026-08-16 —
`architect-reviewer` нашёл `Order.status` (см. `CODING_PROTOCOL.md`
§4b), явный запрос владельца: убедиться, что это не единственный
случай в проекте

По образцу найденного в `OrderRequestDTO`/`OrderService.create()`
(свободное поле `status`, копируется из клиента как есть, ничем не
перезаписывается сервером) — пройти по всем `*RequestDTO` в
`backend/src/main/java/com/be/web/dto/request/` и для каждого поля,
которое ПО СМЫСЛУ должно решать приложение, а не клиент (статус, флаг
подтверждения/оплаты, id владельца, роль, любое "административное"
состояние) — убедиться, что соответствующий `*Service.create()`/
`update()` либо игнорирует это поле у обычного юзера, либо явно
проверяет права через `@PreAuthorize`/сервисную проверку перед тем как
довериться значению. Отдельно проверить симметрично: нет ли уже
исправленных ранее в этой же сессии мест (Group/Course/Workshop
`update()`), которые случайно НЕ покрывают новые поля, добавленные
после фикса.

Результат — не "один диф", а список конкретных находок (или
подтверждение "чисто") по каждому DTO, с последующими точечными
фиксами там, где реально нашлось.

---

## LR-090 — `GroupDTO.enrolledCount` разошёлся с реальным `capacityLeft` после LR-084

**Tier:** LOW (отображение/данные, не безопасность)
**Статус:** Open · backlog
**Источник:** `architect-reviewer`, ревью бэкенда LR-084, 2026-08-16

`GroupMapper.enrolledCount` считает `group.getEnrollments().size()` —
ВСЕ enrollments, включая `CANCELLED`/`EXPIRED`. До LR-084 это уже было
немного неточно (отмена никогда не "освобождала" видимое место), но
не расходилось явно ни с чем. После LR-084 `capacityLeft` стал
настоящим источником истины (атомарно инкрементируется на
cancel/expire) — теперь после любой отмены эти два числа реально
покажут разное: `capacityLeft` правильно покажет освободившееся место,
а любой UI на `enrolledCount` — нет.

**Сделать:** пересчитать `enrolledCount` из `capacity - capacityLeft`
(или явно отфильтровать по статусу), свериться, что оба числа нигде не
показываются рядом как будто согласованные.

---

